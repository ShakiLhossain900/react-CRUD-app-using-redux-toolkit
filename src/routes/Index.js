import React from "react";
import { BrowserRouter, Route, Routes } from "react-router-dom";

const Index = () => {
  return (
    <BrowserRouter>
      <Routes>
        <Route path="/"  element={}/>
      </Routes>
    </BrowserRouter>
  );
};

export default Index;
